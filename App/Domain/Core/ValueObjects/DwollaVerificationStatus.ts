export enum DwollaVerificationStatus {
  VERIFIED = 'verified',
  RETRY_VERFICATION = 'retry',
  DOCUMENT_NEEDED = 'document',
  DEACTIVATED = 'deactivated',
  SUSPENDED = 'suspended',
  REACTIVATE = 'reactivated',
}
